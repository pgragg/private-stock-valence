require 'rails_helper'

describe CachedMechanize do
  let(:base_path) {'test'}
  it 'creates a file' do
    agent = OpenStruct.new({})
    agent = CachedMechanize.new(agent, )
    expect().to eq()
  end

end